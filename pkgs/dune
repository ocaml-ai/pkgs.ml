(executable
 (public_name pkgs_ml)
 (name pkgs)
 (preprocess
  (pps bytestring.ppx html_of_jsx.ppx ppx_yojson_conv serde_derive))
 (libraries
  trail
  nomad
  blink
  unix
  html_of_jsx.lib
  typesense_blink
  serde
  serde_sexpr
  serde_json
  sexplib
  stdune
  ppx_yojson_conv))

(env
 (docker
  (flags
   (:standard -ccopt -static -cclib -no-pie))))

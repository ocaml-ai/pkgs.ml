(executable
 (public_name pkgs_ml)
 (name pkgs)
 (preprocess
  (pps bytestring.ppx html_of_jsx.ppx ppx_yojson_conv))
 (libraries
  trail
  nomad
  blink
  unix
  html_of_jsx.lib
  typesense
  cohttp
  cohttp-lwt-unix
  ppx_yojson_conv))

(env
 (docker
  (flags
   (:standard -ccopt -static -cclib -no-pie))))
